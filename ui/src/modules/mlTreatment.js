import angular from 'angular';

angular.module('mlTreatment', [])
  .controller('mlTreatmentController', ['$scope', '$http', '$state', 'localStorageService', function($scope, $http, $state, localStorageService) {

  }]);