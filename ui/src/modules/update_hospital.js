import angular from 'angular';

angular.module('updateHospital', [])
  .controller('updateHospitalController', ['$scope', '$http', '$state', '$rootScope', function($scope, $http, $state, $rootScope) {
    
  }]);